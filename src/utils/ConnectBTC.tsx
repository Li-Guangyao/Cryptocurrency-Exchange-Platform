import {BTC_Endpoint} from "../env.development";
import {bitcoin, testnet, regtest} from "bitcoinjs-lib/src/networks";


export const sendBTCTxnToMe = (sender: string, amount: number) => {

}

export const sendBTCTxn = (receiver: string, amount: number) => {
    testnet.bech32
}